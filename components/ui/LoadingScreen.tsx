'use client';import{AnimatePresence,motion}from'framer-motion';import{useEffect,useState}from'react';export default function LoadingScreen(){const[show,setShow]=useState(true);useEffect(()=>{const t=setTimeout(()=>setShow(false),700);return()=>clearTimeout(t)},[]);return <AnimatePresence>{show&&<motion.div initial={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[100] grid place-items-center bg-[var(--bg)]"><div className="text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-xl bg-cyan-400/10 font-black text-cyan-400 ring-1 ring-cyan-400/30">SN</div><div className="mt-4 font-mono text-xs text-slate-500">Initializing portfolio…</div></div></motion.div>}</AnimatePresence>}
