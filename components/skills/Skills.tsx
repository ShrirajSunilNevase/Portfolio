'use client';
import { motion } from 'framer-motion';import { skillGroups } from '@/data/skills';
const icons=['◈','⌘','◉','◇','✦'];
export default function Skills(){return <section id="skills" className="section border-y" style={{borderColor:'var(--border)'}}><div className="container-x"><div className="eyebrow">Skills</div><h2 className="section-title">Tools I use to turn ideas into working products.</h2><p className="section-copy">A practical stack across frontend, backend, databases and development tooling.</p><div className="mt-12 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{skillGroups.map((g,i)=><motion.div key={g.title} initial={{opacity:0,y:20}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-60px'}} transition={{delay:i*.06}} className="card p-6"><div className="flex items-center justify-between"><h3 className="font-bold">{g.title}</h3><span className="text-cyan-400">{icons[i]}</span></div><div className="mt-5 flex flex-wrap gap-2">{g.items.map(s=><span key={s} className="rounded-lg border px-3 py-2 text-xs font-semibold" style={{borderColor:'var(--border)',color:'var(--muted)'}}>{s}</span>)}</div></motion.div>)}</div></div></section>}
